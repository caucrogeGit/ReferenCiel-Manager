# Dépendances de développement du projet.
# Standard aligné sur Forge (ADR-005/006). Runtime de l'app = requirements.txt (100% Forge).
-r requirements.txt
-r requirements-docs.txt

# Tests & qualité
pytest>=8.0
ruff>=0.4
pyright>=1.1.400
types-PyYAML>=6.0  # stubs pour le typage strict des tests méta (lecture mkdocs.yml)
pip-audit>=2.0
bandit>=1.7

# Infrastructure de test partagée Forge (ADR-041) : FakeRequest + plugin pytest.
# Épinglée au MÊME commit que forge-mvc (voir requirements.txt) pour garantir la
# compatibilité de version. Outil de développement — jamais importé à l'exécution.
forge-mvc-testing @ git+https://github.com/caucrogeGit/Forge.git@40b9d7b8e93b110c5ad2a82dd13775a9dc966a2e#subdirectory=packages/forge-mvc-testing
