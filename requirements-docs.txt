# Dépendances de la documentation du projet (mkdocs).
# Chaîne alignée sur celle de Forge (ADR-005) pour un standard identique.
# Installer avec : pip install -r requirements-docs.txt
mkdocs>=1.6
mkdocs-material>=9.7
pymdown-extensions>=10.0
mkdocs-glightbox>=0.5
