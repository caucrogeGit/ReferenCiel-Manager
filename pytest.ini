[pytest]
testpaths = tests
addopts = --strict-markers
markers =
    meta: test de cohérence projet (doc, ADR, nav mkdocs) — pas un test métier
    smoke: test de fumée (structure Forge conforme, imports) sans backend BDD
    db: test d'intégration nécessitant un backend BDD installé (sauté sans backend)
